type Point {
    self.x = 0;
    self.y = 0;

    getX():number => x;
    getY():number => y;

    setX(x:number) => x := x;
    setY(y:number) => y := y;
}

type Persona(first:string, last:string) {
    self.first=first;
    self.last=last;
    get_full():string => first@@last;
    calculaEdad(edad:number) {
        if(edad<18)
        {
            print("Menor de edad");
        }
        elif(true)
        {
            print("No es nada");
        }
        
    }
}