{
    let total = { print("Total"); 5; } + 6 in print(total);
    let x = new A(); 
    x = {if (rand() < 0.5) new B() else new C() in
        if (x is B)
            let y : B = x as B in {
                y.hello();
            }
        else {
            print("x cannot be downcasted to B");
        };
    }
}